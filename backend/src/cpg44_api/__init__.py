"""CPG44 product API — FastAPI service inspired by the Football CV platform.

The vision engine (`soccer_analytics`) stays independent. This package is the
web contract: REST + WebSocket, wearable join key, dashboard-facing snapshots.
"""

__version__ = "0.2.0"
